{% extends "layout.html.twig" %}

{% block body_main %}
    {{ form_start(form, {'attr': {'class': 'form-horizontal'}}) }}
        {{ form_errors(form) }}

        <div class="form-group">
            {{ form_row(form.textCrypt) }}
        </div>
        <div class="form-group">
            {{ form_row(form.save) }}
        </div>
    {{ form_end(form) }}
{% endblock body_main %}
